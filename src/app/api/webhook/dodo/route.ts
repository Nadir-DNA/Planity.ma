/**
 * Alias de /api/webhooks/dodo — l'endpoint configuré dans le dashboard Dodo
 * pointe sur /api/webhook/dodo (singulier). Même handler, mêmes garanties.
 */
export { POST, GET } from "@/app/api/webhooks/dodo/route";
